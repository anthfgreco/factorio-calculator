# Core

Pure deterministic TypeScript. This directory must run in Node and may import only other core modules.

Put math, solver logic, normalized data contracts, and deterministic transformations here. Never use DOM, D3, browser globals, application state, icons, or runtime classes.
