# Runtime

Browser-facing models created from validated calculator datasets. These classes preserve legacy UI compatibility and may carry icons or dropdown behavior.

Do not add new solver or selection policy here. Move deterministic rules into core or application instead.
