# Presentation primitives

Neutral DOM components shared by runtime models, the UI, and visualizations: icons, tooltips, and dropdown helpers.

This layer must not depend on calculator application state, runtime domain models, or feature-specific UI modules.
